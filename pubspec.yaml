name: corpus_vitae
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8              # Cupertino Assets for IOS Design
  fl_chart: ^0.69.0                    # Flutter Chart Library for Graphs
  flutter_riverpod: ^0.14.0+3          # Flutter State Management Library
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

